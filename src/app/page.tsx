import {WeatherScreen} from '@/components/weather-screen'

export default function Home() {
	return (
		<main className="screen-stage">
			<div className="screen-scale"><WeatherScreen /></div>
		</main>
	)
}
